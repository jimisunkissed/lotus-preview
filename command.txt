# supabase
npx supabase gen types typescript --project-id {project-id} --schema public > src/types/supabase/supabase-data-type.ts
dotenv -e .env -- bash -c 'npx supabase gen types typescript --project-id $NEXT_PUBLIC_SUPABASE_PROJECT_ID --schema public > src/types/supabase/supabase-data-type.ts'


# fly
1. fly launch
2. remove docker-entrypoint.js
3. remove ENTRYPOINT from dockerfile
4. check fly.toml
5. check .github/workflows/fly-deploy.yml
6. fly secrets import < .env
7. npx dockerfile --mount-secret=ENV_KEY_1
8. fly deploy \
--build-secret NEXT_PUBLIC_SUPABASE_URL="$(grep NEXT_PUBLIC_SUPABASE_URL .env | cut -d '=' -f2)" \
--build-secret NEXT_PUBLIC_SUPABASE_KEY="$(grep NEXT_PUBLIC_SUPABASE_KEY .env | cut -d '=' -f2)"
